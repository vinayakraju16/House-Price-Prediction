# Full local/dev environment: backend runtime plus ml/ (EDA plotting needs matplotlib).
# The production backend Docker image installs requirements-backend.txt instead, which
# excludes matplotlib since it is never imported by backend/ code.
-r requirements-backend.txt
matplotlib==3.10.5
