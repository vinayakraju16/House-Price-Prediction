[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = [
  "backend/migrations",
  "backend/api/migrations",
  "ml/notebooks",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["api", "djanoproject", "ml", "model_runtime"]
